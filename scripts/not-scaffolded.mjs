#!/usr/bin/env node
/**
 * Заглушка для скриптов, которые не могут работать до установки приложений.
 * Печатает честную причину и падает — чтобы «команда есть, но ничего не делает»
 * не выглядело как успех.
 */
const [task = 'task', reason = 'ещё не реализовано'] = process.argv.slice(2);

console.error(`\n  ✗ pnpm ${task}: ${reason}.`);
console.error('    Это заглушка скелета, а не сломанная сборка.');
console.error('    Установку выполняет агент-владелец соответствующего слоя');
console.error('    (см. agent_readme.md, раздел «Состав команды агентов»).\n');
process.exit(1);
