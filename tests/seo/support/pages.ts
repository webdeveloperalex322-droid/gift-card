/**
 * Инвентарь страниц, которые сайт отдаёт СЕЙЧАС, и ожидаемые факты о каждой:
 * директива робота, состав изображений, вид структурированных данных, наличие
 * `<meta name="description">`.
 *
 * Список ведётся только добавлением: страницы Э3-09…Э3-11 (главная, `/search`,
 * информационные) дописаны сюда, и все инвариантные specs (`single-h1`,
 * `self-canonical-absolute`, `robots-directive-present`,
 * `unique-title-h1-description`, `internal-links-are-anchors`,
 * `server-rendered-without-js`, `no-client-javascript`, `image-attributes`,
 * `structured-data-matches-visible`, `canonical-ignores-query`,
 * `status-ignores-query`, `robots-not-opened-by-query`, `site-nav-present`)
 * начинают проверять их без единой правки в самих specs. Заглушек-скипов на ещё
 * не существующие страницы здесь нет намеренно: пропуск, притворяющийся
 * проверкой, — та же ошибка отчётности, что и `SKIPPED`, выданный за `PASSED`.
 *
 * ## Главное правило этого файла: сюда попадает только то, что гарантирует КОД
 *
 * В инвентарь входит страница, которая отвечает 200 при ЛЮБОМ состоянии базы:
 * маршрут Astro со своим текстом. Страницы, существующие только у
 * опубликованной записи (карточка, подборка, каталоги `/otkrytki` и `/podborki`,
 * любая страница пагинации), сюда не входят — на пустой базе они справедливо
 * отвечают 404, и запись о них красила бы `pnpm verify` у того, у кого база
 * пуста. Разбор — в блоке «ПОЧЕМУ ЗДЕСЬ НЕТ КАРТОЧКИ, ПОДБОРОК И КАТАЛОГОВ»
 * ниже.
 *
 * Замерено 2026-08-27 на локальной базе: в ней ЛЕЖАТ опубликованные записи —
 * следы смоука Э3-05 (`Смоук Э3-05: группа`, 25 открыток `smouk-e3-05-otkrytka-*`).
 * Поэтому `/otkrytki`, `/podborki` и страницы пагинации на ЭТОЙ машине отвечают
 * 200, а на чистой базе ответят 404. Из этого прямо следует правило абзацем
 * выше: инвентарь не имеет права зависеть от содержимого базы, иначе результат
 * `pnpm verify` зависит от того, кто последним гонял смоук.
 *
 * ## Про поле `expectedRobots`
 *
 * Оно фиксирует, что страница отдаёт СЕЙЧАС, и охраняет границу: перевод
 * страницы в `index,follow` — решение человека (п. 7.1 и п. 23 ТЗ), а не
 * побочный эффект правки шаблона. Если рефакторинг layout молча откроет
 * страницу в индекс, `robots-directive-present.spec.ts` упадёт. Обратная
 * сторона: когда человек ОСОЗНАННО открывает страницу, он правит и эту строку —
 * то есть решение видно в дифе. Аудитор значения здесь не «повышает»: смена
 * `noindex` на `index` в этом файле без решения человека — нарушение границ
 * агента `seo-auditor`.
 */

/**
 * Директивы, допустимые в `<meta name="robots">` на этом сайте.
 *
 * ## Почему это копия набора и почему она копией остаётся
 *
 * С задач Э4-05 и Э4-06 набор живёт в `@otkritka/shared`
 * (`ROBOTS_DIRECTIVES`) в единственном экземпляре: `apps/cms` строит из него
 * поле записи, `apps/web` — проверку директивы и отбор в sitemap, своих списков
 * ни у того, ни у другого больше нет. Внутри ПРОДУКТА это правильно: две копии
 * расходятся молча. Приёмка — случай другой, и переход на общий импорт здесь
 * рассмотрен и ОТКЛОНЁН.
 *
 * Довод один, но решающий: приёмка не имеет права брать эталон из кода, который
 * проверяет. Импортируй она `ROBOTS_DIRECTIVES`, добавление в общий набор нового
 * значения — скажем, `index,nofollow` — приёмка приняла бы АВТОМАТИЧЕСКИ:
 * страница, начавшая его отдавать, прошла бы `robots-directive-present.spec.ts`
 * без единого падения и без строки в дифе приёмки. Директива управляет
 * попаданием страницы в индекс, расширять набор без решения человека нельзя
 * (шапка `packages/shared/src/robots.ts` говорит это прямо), — а молчаливое
 * согласие приёмки как раз и превратило бы правку кода в такое решение.
 *
 * Расхождение при этом не остаётся незамеченным, и направление у него безопасное:
 *
 *   - значение ДОБАВИЛИ в общий набор и страница его отдала — приёмка падает с
 *     «значение не входит в список допустимых директив проекта». Это и нужно:
 *     человек видит новое значение и решает, законно ли оно;
 *   - значение УБРАЛИ из общего набора — приёмка остаётся строже кода. Ложного
 *     зелёного не возникает: страница такого значения уже не печатает, а
 *     проверять по списку, в котором лишняя строка, безопасно.
 *
 * То есть копия здесь самосторожащая, тогда как импорт снял бы сторожа. Тождество
 * наборов внутри продукта проверяет `tests/unit/robots.test.ts` — там оно и
 * должно проверяться.
 */
export const ALLOWED_ROBOTS_DIRECTIVES = [
  'index,follow',
  'noindex,follow',
  'noindex,nofollow',
] as const;

export type RobotsDirective = (typeof ALLOWED_ROBOTS_DIRECTIVES)[number];

/**
 * Что страница обязана содержать по части изображений.
 *
 * Поле объявляется в инвентаре, а не выводится из ответа, по той же причине, что
 * и {@link AcceptancePage.expectedRobots}: проверка «у всех `<img>` есть
 * `width`, `height`, `alt`, и ровно одно изображение не ленивое» на странице БЕЗ
 * изображений выполняется тривиально, то есть spec выглядел бы работающим,
 * ничего не проверяя. Объявление разделяет три состояния:
 *
 *   - `none` — изображений на странице нет НИ ПРИ КАКОМ состоянии базы, и это
 *     утверждение проверяется: появление `<img>` валит spec с требованием
 *     объявить страницу заново. Молчаливо получить непроверенную картинку на
 *     первом экране нельзя;
 *   - `primary` — есть первое крупное изображение (LCP-кандидат) плюс, возможно,
 *     ленивые. Тогда работает весь контракт раздела «Изображения»;
 *   - `data-driven` — состав изображений определяют ОПУБЛИКОВАННЫЕ ЗАПИСИ, а не
 *     маршрут. Так устроена главная: блок свежих открыток печатается, только
 *     если открытки есть. Это НЕ поблажка: при непустой сетке работает весь
 *     контракт `primary`, а при пустой проверяется, что и сетки на странице нет
 *     — плитки без `<img>` (изображение потерялось на рендере) валят spec.
 *     Состояние «нет ни сетки, ни изображений» законно только на пустой базе, и
 *     spec помечает такой прогон аннотацией, чтобы это было видно в отчёте.
 */
export type ImageExpectation = 'none' | 'primary' | 'data-driven';

/**
 * Какой вид структурированных данных (`application/ld+json`) страница отдаёт.
 *
 * Три состояния, и каждое проверяется УТВЕРЖДЕНИЕМ, а не пропуском:
 *
 *   - `none` — разметки нет вовсе; появление блока валит spec, иначе разметка,
 *     приехавшая с правкой layout, осталась бы непроверенной;
 *   - `site` — документ описывает САЙТ, а не список: узел `WebSite` (ТЗ §5.2 —
 *     «JSON-LD: WebSite + Organization»), при заполненном глобале рядом
 *     `Organization` (Ч-17). Элементов списка (`ListItem`) на такой странице нет
 *     и быть не должно: крошек у главной нет по ТЗ §7.6, а `ItemList` §5.2 от
 *     главной не требует;
 *   - `list` — документ описывает ВИДИМЫЙ СПИСОК: `BreadcrumbList` у любой
 *     страницы глубже главной (ТЗ §7.6) и/или `ItemList` подборки и каталога
 *     (ТЗ §5.3). Тогда включается сверка каждого `ListItem` с видимым текстом и
 *     ссылками страницы.
 *
 * ## Почему появилось третье состояние, а не «ListItem обязателен, когда в
 * документе есть WebPage/CollectionPage»
 *
 * Оба варианта лечат одно падение: главная (Э3-09) отдаёт `WebSite`, а инвентарь
 * объявлял `/` как `none`. Выбран третий вид объявления, потому что условие
 * «обязателен, когда есть WebPage» ослабляет проверку молча. При нём страница,
 * у которой разметка деградировала до одного `WebSite` (например, у подборки
 * пропали и крошки, и `ItemList`), проходила бы приёмку: узла `WebPage` в
 * документе нет — значит `ListItem` и не требуется. Объявление же говорит, чего
 * от страницы ЖДУТ, и любое расхождение с ожиданием — падение с требованием
 * объявить страницу заново. Ровно этим свойством ценно и состояние `none`.
 *
 * Второе, менее очевидное: `site` не просто «не требует списка», а ЗАПРЕЩАЕТ
 * `ListItem`. Появление крошек на главной — расхождение с ТЗ §7.6, а появление
 * `ItemList` — разметка, которой ТЗ не просит; и то и другое обязано быть
 * замеченным, а не молча принятым.
 */
export type StructuredDataExpectation = 'none' | 'site' | 'list';

/**
 * Есть ли на странице `<meta name="description">`.
 *
 * Поле пришлось объявить, когда в инвентарь вошли информационные страницы
 * (Э3-11). У них description ПУСТ, потому что текст берётся из глобала настроек,
 * а глобал — заглушка (Ч-19), и правило проекта «пустое поле — значит тега нет
 * вовсе» (Ч-10, Ч-17, шапка `BaseLayout.astro`) убирает тег целиком.
 *
 * Дальше было ровно два честных выхода, и подгонка теста не входила ни в один:
 *
 *   1. требовать непустой description на каждой странице выборки. Тогда
 *      приёмка требовала бы, чтобы АГЕНТ придумал описание страницы «Условия
 *      использования» вместо человека, — то есть шаблонный SEO-текст, прямой
 *      запрет п. 23.4 и обход Ч-19;
 *   2. объявить факт «тега нет» и проверять его утверждением — как у
 *      `images: 'none'`. Выбрано это.
 *
 * `absent` держит проверку в обе стороны: description, появившийся у страницы,
 * валит spec (значит человек заполнил глобал — факт надо объявить заново), а
 * страница, объявленная `absent` и при этом ИНДЕКСИРУЕМАЯ, валит spec всегда: у
 * индексируемой страницы description обязателен (п. 22.1). То есть в момент,
 * когда человек по Ч-23 откроет `/o-proekte` в `index,follow`, приёмка потребует
 * заполненного описания — а не промолчит.
 */
export type DescriptionExpectation = 'present' | 'absent';

export interface AcceptancePage {
  /**
   * Канонический путь страницы: без завершающего слеша (решение Ч-21),
   * единственное исключение — корень `/`.
   */
  readonly path: string;
  /** Ожидаемое значение `<meta name="robots">`. */
  readonly expectedRobots: RobotsDirective;
  /** Ожидаемый состав изображений. */
  readonly images: ImageExpectation;
  /** Ожидаемый вид структурированных данных. */
  readonly structuredData: StructuredDataExpectation;
  /** Ожидаемое наличие `<meta name="description">`. */
  readonly description: DescriptionExpectation;
  /** Задача, которой страница принадлежит, — чтобы падение указывало на владельца. */
  readonly task: string;
  /** Зачем страница существует и что на ней проверяется. */
  readonly note: string;
}

export const ACCEPTANCE_PAGES: readonly AcceptancePage[] = [
  {
    path: '/',
    expectedRobots: 'noindex,follow',
    images: 'data-driven',
    structuredData: 'site',
    description: 'present',
    task: 'Э3-09',
    note:
      'Главная. Отвечает 200 при любом состоянии базы: её содержание не сводится к записям — ' +
      'заголовок, вводный блок и форма поиска приходят из кода (`src/seo/home-page.ts`). ' +
      'Разметка — `WebSite` (ТЗ §5.2) плюс `Organization` при заполненном глобале (Ч-17), ' +
      'списков в ней нет: крошек у главной нет по ТЗ §7.6, `ItemList` §5.2 не требует — ' +
      'отсюда `structuredData: site`. Изображения зависят от данных: блок свежих открыток ' +
      'печатается только при опубликованных открытках, поэтому `images: data-driven` (замерено ' +
      '2026-08-27: на локальной базе со следами смоука Э3-05 на главной 12 плиток и контракт ' +
      'изображений проверяется целиком; на чистой базе блока нет вовсе). После Э3-13, когда ' +
      'наполнение каталога станет условием задачи, а не побочным следствием чужого смоука, ' +
      'объявление становится `images: primary` — сетка свежих открыток на главной будет ' +
      'всегда. Условиям п. 5.1 главная пока не удовлетворяет (спрос данными не подтверждён, ' +
      'Ч-04-1), поэтому `noindex,follow`; открыть её в индекс может только человек.',
  },
  {
    path: '/search',
    expectedRobots: 'noindex,follow',
    images: 'none',
    structuredData: 'list',
    description: 'present',
    task: 'Э3-10',
    note:
      'Внутренний поиск. `noindex` БЕЗУСЛОВНО (ТЗ §5.5): это не решение, которое человек мог ' +
      'бы поменять, поэтому запись здесь охраняет константу `SEARCH_ROBOTS`, а не чьё-то ' +
      'решение. Изображений на КАНОНИЧЕСКОМ адресе нет ни при каком состоянии базы: поиск ' +
      'выполняется только при непустом `?q=`, а `/search` без параметра показывает форму — ' +
      'отсюда `images: none`, и это утверждение, а не следствие пустой базы. Состав выдачи ' +
      'при заданном `?q=` от базы зависит, и адреса с параметрами в инвентарь не входят вовсе: ' +
      'у них нет своего canonical (ТЗ §6.5), их проверяют `search-not-indexable.spec.ts` и ' +
      '`canonical-ignores-query.spec.ts`. Разметка — `BreadcrumbList` (ТЗ §7.6), отсюда ' +
      '`structuredData: list`.',
  },
  {
    path: '/o-proekte',
    expectedRobots: 'noindex,follow',
    images: 'none',
    structuredData: 'list',
    description: 'absent',
    task: 'Э3-11',
    note:
      'Информационная страница — статический маршрут Astro, а не запись CMS. Именованное ' +
      'исключение Ч-23 даёт ей `index,follow` и место в sitemap ТОЛЬКО при заполненном ' +
      'реальном тексте; пока стоит заглушка, страница остаётся `noindex,follow` и вне sitemap. ' +
      'Текст и description приходят из глобала настроек, глобал не заполнен — тега description ' +
      'нет вовсе, отсюда `description: absent` (разбор — в шапке DescriptionExpectation).',
  },
  {
    path: '/usloviya',
    expectedRobots: 'noindex,follow',
    images: 'none',
    structuredData: 'list',
    description: 'absent',
    task: 'Э3-11',
    note:
      'Условия использования. Тот же случай, что `/o-proekte`: заглушка, Ч-23, ' +
      '`description: absent`. Отдельная тонкость этой страницы: на неё ссылается ' +
      '`acquireLicensePage` в разметке карточки, и эта ссылка доводом за индексацию НЕ ' +
      'является — она работает и на неиндексируемой странице (`CLAUDE.md`, «Sitemap и robots»).',
  },
  {
    path: '/kontakty',
    expectedRobots: 'noindex,follow',
    images: 'none',
    structuredData: 'list',
    description: 'absent',
    task: 'Э3-11',
    note: 'Контакты. Тот же случай, что `/o-proekte`: заглушка, Ч-23, `description: absent`.',
  },
];

/*
 * ПОЧЕМУ ЗДЕСЬ НЕТ КАРТОЧКИ, ПОДБОРОК И КАТАЛОГОВ (Э3-05…Э3-08), хотя шаблоны
 * готовы и на этой машине отвечают 200.
 *
 * Их страницы существуют только у ОПУБЛИКОВАННОЙ записи. Каталог без записей
 * отвечает 404 намеренно (пустая страница не отдаёт 200, ТЗ §5.3 — см. шапку
 * `apps/web/src/data/catalog.ts`), подборка без открыток — тоже. То есть статус
 * этих адресов — факт о СОДЕРЖИМОМ БАЗЫ, а не о коде, и запись о них в инвентаре
 * означала бы, что `pnpm verify` красный у всех, у кого база пуста, и зелёный у
 * того, кто вчера гонял смоук. Приёмка обязана отвечать на вопрос «что делает
 * код», а не «что лежит в чьей-то базе».
 *
 * Наполнение — задача Э3-13, и упирается она в человека: ≥ 120 реальных открыток
 * агент не создаёт.
 *
 * Вариант «завести записи на временных фикстурах» рассмотрен и ОТКЛОНЁН, причины
 * по убыванию веса:
 *
 *   1. `pnpm test:seo` входит в `pnpm verify`, который гоняет каждый агент на
 *      каждой задаче. Сеялка внутри приёмки означала бы, что стандартная команда
 *      проверки ПУБЛИКУЕТ записи в базу контента. Публикация — решение человека
 *      (п. 7.1 и п. 23 ТЗ); в одноразовом скрипте, который разработчик запускает
 *      сам (`apps/web/scripts/smoke-pages.ts`), это допустимо, а как побочный
 *      эффект `pnpm verify` — нет. Оборванный прогон при этом оставляет
 *      опубликованные записи в базе — и это не гипотеза: замер 2026-08-27 нашёл
 *      в локальной базе именно такие следы (`Смоук Э3-05: группа` и 25 открыток
 *      `smouk-e3-05-otkrytka-*`), из-за которых главная отдаёт непустую сетку;
 *   2. проверки на своих же фикстурах частью тавтологичны. «title уникален и
 *      непуст» на данных, которые сеялка сама и написала, доказывает сеялку, а не
 *      сайт; настоящую силу это утверждение получает только на реальном
 *      каталоге, где шаблонный title «Открытки на <тема>» и порождает дубли;
 *   3. проверки, которые на фикстурах ДЕЙСТВИТЕЛЬНО работают (один H1, одно
 *      неленивое изображение, `ItemList` = видимая сетка, ноль клиентского JS,
 *      301 на слеш, 404 у черновика), уже сделаны против собранного сервера в
 *      `apps/web/scripts/smoke-pages.ts` — включая браузер с отключённым JS.
 *      Вторая копия тех же фикстур с другим владельцем добавила бы не покрытие,
 *      а расхождение;
 *   4. приёмка стала бы зависеть от PostgreSQL, конфига CMS и пайплайна
 *      изображений — то есть краснела бы по причинам, к состоянию сайта не
 *      относящимся. Этот довод уже принят в `support/serve-built-web.mjs`
 *      (проект `tests/` намеренно не собирается перед стартом сервера).
 *
 * Что вместо этого: specs написаны так, что страница входит в проверку ОДНОЙ
 * записью в этом списке. Состав, который добавляется на Э3-13, известен заранее —
 * `/otkrytki`, `/otkrytki/<slug>`, `/otkrytki/page/2`, `/podborki`,
 * `/podborki/prazdniki`, `/podborki/prazdniki/<tema>`,
 * `/podborki/prazdniki/<tema>/<adresat>`, `/podborki/adresaty/<adresat>`, у всех
 * `expectedRobots: 'noindex,follow'`, `images: 'primary'`,
 * `structuredData: 'list'`. Обязательное условие для каждой добавляемой
 * страницы: у записи ЗАПОЛНЕН `metaDescription` (`description: 'present'`). При
 * пустом поле тега `<meta name="description">` нет вовсе, и объявить такую
 * страницу пришлось бы как `description: 'absent'` — что для страницы,
 * претендующей на индексацию, spec не пропустит (п. 22.1), а подставлять
 * описание шаблоном запрещено (п. 23.4).
 */

/** Индексируемая страница — та, чья директива начинается с `index`. */
export function isIndexable(page: AcceptancePage): boolean {
  return page.expectedRobots.startsWith('index');
}

/**
 * Ожидаемый путь в self-canonical. Совпадает с {@link AcceptancePage.path}: у
 * корня канонический абсолютный адрес — `<origin>/`, у остальных страниц —
 * `<origin><path>` без завершающего слеша.
 */
export function expectedCanonicalPath(page: AcceptancePage): string {
  return page.path;
}

/**
 * Наборы параметров, которыми проверяется независимость страницы от строки
 * запроса (ТЗ §6.5, §5.5; вето V6).
 *
 * Здесь ровно четыре класса, и каждый попадал в индекс в реальных проектах:
 *
 *   - метки внешних кампаний (`utm_*`) — приезжают из чужих ссылок и не имеют
 *     отношения к содержимому;
 *   - фильтр представления (`format`) — единственный ЗНАЧАЩИЙ параметр сайта
 *     (`apps/web/src/routing/view-params.ts`);
 *   - параметр, которого у сайта нет (`style`) — так выглядит выдумка краулера
 *     или чужого скрипта; страница обязана остаться собой;
 *   - смесь с попыткой пагинации параметром (`page=2&filter=x`) — форма,
 *     которую решение Ч-05 запрещает как адрес; она не должна ни менять
 *     canonical, ни менять статус.
 */
export const QUERY_VARIANTS: readonly { readonly query: string; readonly note: string }[] = [
  { query: '?utm_source=newsletter&utm_medium=email', note: 'метки внешней кампании' },
  { query: '?format=vertical', note: 'фильтр представления по формату' },
  { query: '?style=akvarel', note: 'параметр, которого у сайта нет' },
  { query: '?page=2&filter=x', note: 'пагинация параметром (запрещена Ч-05) плюс мусор' },
];
