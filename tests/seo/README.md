# tests/seo — приёмочные SEO-тесты (контур обратной связи)

Владелец: агент `seo-auditor`. В коде приложений он только читает: найденное
нарушение называет, исправляет владелец слоя.

Тесты кодируют чек-лист приёмки из п. 22 ТЗ. Каждое требование — отдельный spec,
чтобы падение называло конкретное нарушение, а не «SEO сломано».

## Запуск

```bash
BASE_URL=http://127.0.0.1:4321 pnpm test:seo
```

Шлюз `scripts/seo-gate.mjs` печатает один из трёх статусов:

| Статус | Когда |
|---|---|
| `SEO_ACCEPTANCE: PASSED` | Playwright вернул 0 |
| `SEO_ACCEPTANCE: FAILED` | Playwright вернул не 0 |
| `SEO_ACCEPTANCE: SKIPPED` | нет spec-файлов или не задан `BASE_URL` |

**Пропуск не равен прохождению.** Статус переносится в отчёт о завершении задачи
дословно, вместе с причиной.

## Что поднимает сервер

Конфиг `playwright.config.ts` сам запускает `tests/seo/support/serve-built-web.mjs`:
тот собирает (`tsc -b packages/shared packages/images`, затем
`pnpm --filter @otkritka/web run build`) и поднимает **собранный** сервер
командой `pnpm --filter @otkritka/web run start`. Проект `tests/` он намеренно не
собирает: ошибка типов в чужом юнит-тесте не должна мешать приёмке сообщить о
состоянии сайта — типы всего монорепозитория проверяет `pnpm check`, который в
`pnpm verify` идёт раньше.

**Контракт с `apps/web`** (владелец — `astro-web`). Приёмка не знает, как
приложение поднимает сервер, и знать не должна: устройство слоя уже менялось
(standalone-адаптер → `mode: 'middleware'` со своим Node-сервером), и путь до
точки входа менялся вместе с ним. Нужны ровно два скрипта в
`apps/web/package.json`:

| Скрипт | Что обязан делать |
|---|---|
| `build` | собрать всё, что нужно серверу |
| `start` | поднять production-сервер, читая `HOST`, `PORT` и `SITE_URL` из окружения |

Пока `start` есть, приёмка переживает любую перестройку внутренностей. Если его
нет, используется прежний путь (импорт standalone-точки входа), и при неудаче
скрипт печатает, что именно нужно добавить, — а не «сервер не поднялся».

Против `astro dev` приёмку гонять нельзя: в dev-режиме при
`trailingSlash: 'never'` Astro вместо 301 показывает страницу-предупреждение со
статусом 404 (замерено на astro 7.2.4), и spec на одиночный 301 упал бы на верном
коде. Поэтому `reuseExistingServer` выключен — иначе приёмка могла бы молча
подхватить чужой dev-сервер на том же порту.

Переменные окружения приёмки:

| Переменная | Значение по умолчанию | Зачем |
|---|---|---|
| `BASE_URL` | нет, обязательна | адрес стенда: схема + хост + порт, без пути |
| `SEO_SERVER_PORT` | порт из `BASE_URL`, иначе `4321` | порт, на котором слушает поднимаемый сервер (нужен, если `BASE_URL` идёт через обратный прокси) |
| `SEO_SERVER_HOST` | `127.0.0.1` | интерфейс, на котором слушает поднимаемый сервер |
| `SEO_REUSE_SERVER` | не задана | `1` — приёмка идёт против уже поднятого сервера и своего не запускает |
| `SEO_MAINTENANCE_PORT` | порт стенда + 1 | порт ВТОРОГО сервера, который поднимает `maintenance-mode-503.spec.ts` с `MAINTENANCE_MODE=on`. Нужен, когда приёмку гоняют несколько агентов на одной машине |

### Второй сервер: режим обслуживания

Режим обслуживания — параметр окружения, читаемый процессом, который отвечает
(`apps/web/src/server/maintenance.ts`). Включить его у уже запущенного сервера
снаружи нечем — и это правильное устройство: выключатель, который можно дёрнуть
запросом, однажды будет дёрнут запросом. Поэтому `maintenance-mode-503.spec.ts`
поднимает свой процесс из `beforeAll` — тем же скриптом `start`, на порту
`SEO_MAINTENANCE_PORT`, и переиспользует уже собранный артефакт.

Второй записи `webServer` в конфиге нет намеренно: записи поднимаются
параллельно, а каждая по контракту начинает со СБОРКИ — две сборки писали бы в
один `dist`. Останов идёт по дереву процессов (`taskkill /T` на Windows): обёртка
`start` запускает сервер дочерним процессом, а Node на Windows не доставляет
сигналы, поэтому обычный `kill` оставил бы внука держать порт.

## `SITE_URL` и `BASE_URL` — один хост

`CLAUDE.md`, раздел «SEO-тесты»: на окружении приёмки они указывают на один хост,
иначе self-canonical укажет на другой хост и проверка canonical либо ложно
упадёт, либо будет ослаблена. **Ослаблять её запрещено.**

Как это решено здесь:

1. сервер, который поднимает сама приёмка, получает `SITE_URL` = origin
   `BASE_URL`. Окружение согласовано по построению, а не по договорённости;
2. согласованность всё равно проверяется на живом ответе —
   `environment-hosts-match.spec.ts` сравнивает origin self-canonical с origin
   `BASE_URL`. Этот spec нужен для режима `SEO_REUSE_SERVER=1` (там `SITE_URL`
   сервера приёмке неподконтролен) и ловит появление второго источника хоста в
   шаблонах (`Astro.site`, захардкоженный домен, фолбэк на `localhost`);
3. сравнение canonical идёт по абсолютному адресу целиком — схема, хост, порт,
   путь. Формы «оканчивается на путь» в specs нет.

Не проверяется машиной ровно одно: что значение `SITE_URL` продуктового
окружения совпадает с хостом, на котором сайт реально отдаётся. Локально это
непроверяемо в принципе. Чтобы приёмка шла против `http://otkritka.test` из
решения Ч-02, **нужно действие человека вне репозитория**: отобразить
`otkritka.test` на порт приложения (запись в `hosts` + обратный прокси на порт
из `SEO_SERVER_PORT`) и запускать `BASE_URL=http://otkritka.test pnpm test:seo`.
Пока этого нет, приёмка гоняется по адресу петли — и это указано в отчёте, а не
спрятано.

## Инвентарь страниц

`support/pages.ts` — список страниц, которые сайт отдаёт СЕЙЧАС, и ожидаемое
поведение каждой: директива робота (`expectedRobots`), состав изображений
(`images`), вид структурированных данных (`structuredData`) и наличие
`<meta name="description">` (`description`). Инвариантные specs (`single-h1`,
`self-canonical-absolute`, `robots-directive-present`,
`unique-title-h1-description`, `internal-links-are-anchors`,
`server-rendered-without-js`, `no-client-javascript`, `page-has-no-html-twin`,
`image-attributes`, `structured-data-matches-visible`,
`canonical-ignores-query`, `status-ignores-query`,
`filtered-view-not-indexable`, `site-nav-present`) идут по этому списку, поэтому
новая страница добавляется одной записью, без правок в самих specs.

На 2026-08-27 в инвентаре пять страниц: `/` (Э3-09), `/search` (Э3-10),
`/o-proekte`, `/usloviya`, `/kontakty` (Э3-11).

Заглушек-скипов на ещё не существующие страницы здесь нет намеренно: пропуск,
притворяющийся проверкой, — та же ошибка отчётности, что и `SKIPPED`, выданный за
`PASSED`. Чего приёмка пока не покрывает — перечислено ниже и в отчёте задачи, а
не изображается зелёными тестами.

Поля-ожидания существуют против ТАВТОЛОГИИ. Проверка «у всех `<img>` есть
`width`, `height`, `alt`» на странице без изображений выполняется тривиально:
spec выглядит работающим, не встретив ни одного тега. Поэтому страница
объявляет, что на ней должно быть, и обратное утверждение тоже проверяется —
`images: 'none'` означает «изображений нет», и появление изображения валит spec с
требованием объявить страницу заново.

Состояний по три там, где двух не хватило, и оба третьих появились из замеров, а
не из удобства:

| Поле | Состояния | Что означает третье |
|---|---|---|
| `images` | `none`, `primary`, `data-driven` | `data-driven` — состав изображений определяют ОПУБЛИКОВАННЫЕ записи (блок свежих открыток на главной). Утверждение есть в обеих ветвях: есть изображения — работает весь контракт `primary`; нет — на странице обязано не быть и плиток (`class="card-grid__item"`), потому что плитка без `<img>` означает потерянное изображение, а не пустую выборку. Прогон на пустой выборке помечается аннотацией «проверено нечем» |
| `structuredData` | `none`, `site`, `list` | `site` — документ описывает САЙТ (`WebSite`, при заполненном глобале `Organization`), и элементов списка в нём быть НЕ должно: крошек у главной нет по ТЗ §7.6, `ItemList` §5.2 от неё не требует. `list` — документ описывает видимый список (`BreadcrumbList`, `ItemList`), и каждый `ListItem` сверяется с видимым текстом и ссылками |
| `description` | `present`, `absent` | `absent` — тега нет вовсе, потому что текст берётся из глобала настроек, а глобал — заглушка (Ч-19), и правило «пустое поле — значит тега нет» (Ч-10, Ч-17) убирает тег целиком. Проверяется в обе стороны: появившийся description валит spec, а `absent` у ИНДЕКСИРУЕМОЙ страницы валит spec всегда (п. 22.1) |

Почему `structuredData: 'site'` — отдельное объявление, а не условие «`ListItem`
обязателен, когда в документе есть `WebPage`»: условие ослабляет проверку молча.
При нём страница, у которой разметка деградировала до одного `WebSite` (у
подборки пропали и крошки, и `ItemList`), проходила бы приёмку — узла `WebPage` в
документе уже нет, значит и списка не требуется. Объявление же говорит, чего от
страницы ЖДУТ, и любое расхождение — падение с требованием объявить страницу
заново.

## Почему карточки, подборок и каталогов нет в инвентаре, хотя шаблоны готовы

Их страницы существуют только у ОПУБЛИКОВАННОЙ записи: `/otkrytki/<slug>`,
`/podborki/...`, а также сами каталоги `/otkrytki` и `/podborki` (каталог без
записей отвечает 404 намеренно — пустая страница не отдаёт 200, ТЗ §5.3) и любая
страница пагинации. То есть их статус — факт о СОДЕРЖИМОМ БАЗЫ, а не о коде.

Это не теория. Замерено 2026-08-27 в течение одного часа на одной машине: в
09:35 локальная база содержала следы смоука Э3-05 (`Смоук Э3-05: группа`, 25
открыток `smouk-e3-05-otkrytka-*`), `/otkrytki` отвечал 200, а на главной было 12
плиток; в 10:05 записей уже не было, `/otkrytki` отвечал 404, главная — без
изображений. Приёмка прогнана в ОБОИХ состояниях и в обоих зелёная — ровно
потому, что инвентарь от содержимого базы не зависит. Запись каталога в инвентарь
сделала бы `pnpm verify` красным у всех, у кого база пуста, и зелёным у того, кто
последним гонял смоук.

Наполнение — задача Э3-13, и она упирается в человека.

Вариант «завести временные фикстуры и проверять шаблоны уже сейчас» рассмотрен и
ОТКЛОНЁН. Причины по убыванию веса — полностью они выписаны в шапке
`support/pages.ts`, здесь коротко:

1. `pnpm test:seo` входит в `pnpm verify`, который гоняет каждый агент на каждой
   задаче. Сеялка внутри приёмки означала бы, что стандартная команда проверки
   ПУБЛИКУЕТ записи в базу контента, а оборванный прогон оставляет их
   опубликованными;
2. проверки на своих же фикстурах частью тавтологичны: «title уникален» на
   данных, которые сеялка сама и написала, доказывает сеялку, а не сайт;
3. то, что на фикстурах действительно работает, уже проверено против собранного
   сервера в `apps/web/scripts/smoke-pages.ts` (65 проверок, включая браузер с
   отключённым JS). Вторая копия тех же фикстур с другим владельцем добавила бы
   не покрытие, а расхождение;
4. приёмка стала бы краснеть из-за PostgreSQL, конфига CMS и пайплайна
   изображений — то есть по причинам, к состоянию сайта не относящимся.

Состав, который войдёт в инвентарь на Э3-13, известен заранее и записан там же в
`support/pages.ts` вместе с обязательным условием: у каждой добавляемой страницы
поле `metaDescription` записи должно быть ЗАПОЛНЕНО — при пустом теге
`<meta name="description">` нет вовсе, и spec на уникальность справедливо упадёт.

## Разборщик приёмки проверяется отдельно

`html-parser-contract.spec.ts` проверяет не сайт, а `support/html.ts` — то, ЧЕМ
приёмка видит ответ сервера. Ошибка в разборщике не делает тесты красными, она
делает их неправдивыми: часть проверок начинает утверждать не то, что написано в
их сообщениях. Оба случая в этом spec'е реальны:

- **пустое значение атрибута Astro печатает бескавычно.** `alt=""` приезжает как
  `<img … alt>` (`addAttribute` в `astro/dist/runtime/server/render/util.js`,
  astro 7.2.4). Прежний разбор такой атрибут не находил вовсе, поэтому
  декоративное изображение с законным пустым `alt` выглядело бы как изображение,
  у которого `alt` забыли, — приёмка обвинила бы владельца слоя в нарушении,
  которого нет. Сейчас различаются три состояния: атрибута нет (`null`), атрибут
  есть и пуст (`''`), атрибут есть со значением;
- **`\b` перед именем ловил чужой атрибут.** `data-src` отвечал за `src`. Это
  обратная и более опасная ошибка: изображение, подставляемое скриптом из
  `data-src` (ровно то, что запрещает раздел «Рендеринг»), выглядело бы как
  изображение с настоящим `src`.

Серверу этот spec не нужен: он о разборе строк. Падение в нём говорит о приёмке,
а не о состоянии сайта.

## Чего приёмка пока НЕ проверяет

Список ведётся здесь, чтобы зелёный прогон не читался как «чек-лист п. 22 закрыт».
Каждая строка снимается вместе с задачей, которая создаёт проверяемое.

Общая причина у большинства строк одна: **проверяемое существует только у
опубликованной записи**, а инвентарь приёмки не имеет права зависеть от
содержимого базы (разбор выше). Такие строки снимаются на Э3-13 — и снимаются
одной записью в `support/pages.ts`, без правок в specs.

| Требование п. 22 | Почему не покрыто | Снимется на |
|---|---|---|
| sitemap: только 200 + canonical + indexable, валидный XML, image sitemap | маршрутов `/sitemap*.xml` ещё нет | Э4-03 |
| `robots.txt`: форма `Disallow` (Ч-22), ссылка на sitemap-индекс | маршрута нет | Э4-04 |
| `<img>` с `src`/`width`/`height`/`alt`, первый экран без `lazy` | spec `image-attributes` работает, но полный контракт включается только на непустой сетке (`/` объявлена `images: 'data-driven'`). На пустой базе прогон помечается аннотацией «проверено нечем». Страницы с гарантированным изображением (карточка, подборка) в инвентарь войдут вместе с наполнением | Э3-13 |
| `BreadcrumbList` у карточки и подборки | крошки на служебных страницах (`/search`, `/o-proekte`, `/usloviya`, `/kontakty`) проверяются сейчас: `structuredData: 'list'` сверяет каждый `ListItem` с видимым текстом и ссылками. У карточки и подборки цепочка длиннее и собирается из записей — этого в выборке нет | Э3-13 |
| `ItemList` = видимая сетка; `ImageObject.contentUrl` = `<img src>`; `url` = self-canonical у `WebPage`/`CollectionPage` | ветви `structured-data-matches-visible` для этих узлов написаны, но страниц с ними в выборке нет: их печатают карточка, подборка и каталог | Э3-13 |
| фильтр закрывает ИНДЕКСИРУЕМУЮ страницу | `filtered-view-not-indexable` проверяет «представление не бывает открытее страницы»; ни одной страницы с `index,follow` на сайте нет, поэтому «закрывает» проверить не на чем — закрывать нечего | первая страница с `index,follow` |
| ряд ссылок фильтра: `rel="nofollow"` у адресов с параметрами | ряд печатает только страница подборки. Замер 2026-08-27: на отфильтрованном представлении блок пагинации приписывает `?format=…` к ссылкам соседних страниц БЕЗ `rel="nofollow"` (`apps/web/src/components/Pagination.astro`, проп `query`) — это расхождение с ТЗ §6.5 названо владельцу слоя `astro-web`. Утверждение в spec'е уже есть и сработает, когда список войдёт в выборку | Э3-13 |
| пагинация: `/page/1` → одиночный 301 на базовый URL | ответ возможен только когда базовый URL отдаёт 200, то есть при опубликованных открытках: иначе маршрут отвечает 404, чтобы не отправлять 301 на 404. Форма номера покрыта целиком (`pagination-invalid-number`: `0`, `01`, `-1`, `1.5`, слово, номер за пределами, второй уровень, процентный псевдоним) | Э3-13 |
| пагинация: 200, self-canonical на саму страницу, `noindex,follow`, отсутствие подстроки `/page/1` на базовом URL | страницы списка нет в выборке | Э3-13 |
| canonical и статус при параметрах на карточке, подборке, каталоге | `canonical-ignores-query` и `status-ignores-query` гоняются по инвентарю, а этих страниц в нём нет. На шаблонах то же свойство проверено смоуком (`apps/web/scripts/smoke-pages.ts`) | Э3-13 |
| удалённая карточка не отдаёт 200; перенос — одиночный 301 по данным CMS | карточек и редиректов нет | Э4-02, Э3-13 |
| нет страниц-сирот: ≤ 4 перехода от главной | `site-nav-present` проверяет необходимое условие — ссылки на `/`, `/otkrytki`, `/podborki` есть в HTML каждой страницы выборки и страницы 404. Саму ГЛУБИНУ можно измерить только обходом реальных ссылок, а для обхода нужны записи | Э4-08 |
| страница, которой нет в инвентаре, но которая отдаёт 200 | обнаружить нечем: обхода по ссылкам нет, sitemap нет | Э4-03 + Э4-08 |
| уникальность title/H1/description на РЕАЛЬНОМ каталоге | на пяти страницах выборки утверждение работает, но их тексты лежат в коде. Дубли рождаются там, где title собирается из записи по шаблону «Открытки на \<тема\>» | Э3-13 |
| проверяемая машиной часть условий п. 5.1 | ни одной индексируемой страницы нет — блок specs существует, но тестов не порождает | первая страница с `index,follow` |
| условия п. 5.1 целиком (спрос, интент, наполненность, навигация) | машиной не проверяются вовсе; подтверждает человек, а «подтверждённый спрос» данными не подтверждён (Ч-04-1) | решение человека |
| `X-Robots-Tag: noindex` и Basic Auth на staging (Ч-18) | стенда нет, адрес назначается на этапе 7 | Э7-01 |

Закрыто на этом прогоне (было в таблице выше, снято по замеру):

| Требование | Чем закрыто |
|---|---|
| страница 404 содержит навигацию и ссылку на поиск (ТЗ §5.6) | `not-found-page-content.spec.ts`: 404, ровно один непустой H1, ссылка на `/search` В ТЕЛЕ (а не только в меню), ссылки на `/`, `/otkrytki`, `/podborki`, ОТСУТСТВИЕ `<link rel="canonical">`, директива `noindex` |
| страница 404 у сайта одна | `not-found-body-is-single.spec.ts`: тела семи ветвей кода (промах маршрутизации, маршрут карточки, маршрут ветви подборок, маршрут пагинации, путь под занятым целиком `/search`, промах по `/media`, путь админки) сравниваются байт в байт |
| 503 + `Retry-After` при обслуживании | `maintenance-mode-503.spec.ts` на СВОЁМ сервере с `MAINTENANCE_MODE=on`: 503 на одиннадцати классах адресов, `Retry-After` равен заданному, ОТСУТСТВИЕ 301 на неканонической форме, у тела 503 свой H1 и `noindex,nofollow` |
| поиск не создаёт индексируемых дублей | `search-not-indexable.spec.ts`: десять запросов, включая кириллицу, пробелы, `%` и попытку разметки — везде 200, `noindex`, canonical на чистый `/search`, один и тот же `title` и `H1` |
| URL с параметрами отдаёт canonical на чистую страницу | `canonical-ignores-query.spec.ts` — четыре класса параметров (utm-метки, фильтр `format`, несуществующий параметр, пагинация параметром) на каждой странице выборки |
| набор параметров не меняет статус | `status-ignores-query.spec.ts`, включая обратную сторону: несуществующий адрес с параметрами остаётся 404, а не превращается в soft 404 |
| фильтр не порождает индексируемый URL | `filtered-view-not-indexable.spec.ts`: директива представления не бывает ОТКРЫТЕЕ директивы страницы (порядок задан числами, а не парами значений) |
| `WebSite` на главной есть всегда (ТЗ §5.2) | `structured-data-matches-visible.spec.ts`, ветвь `site`: один узел `WebSite`, `url` совпадает с self-canonical, `name` присутствует в видимом тексте, `ListItem` в документе нет |

## Поле `expectedRobots` в инвентаре

Оно фиксирует, что страница отдаёт сейчас, и охраняет границу: перевод страницы в
`index,follow` — решение человека (п. 7.1 и п. 23 ТЗ). Молчаливое открытие
индексации после правки layout иначе не поймать — в браузере оно выглядит как
раньше. Когда человек открывает страницу осознанно, правка этой строки входит в
его решение и видна в дифе. Аудитор значение не «повышает» сам.
