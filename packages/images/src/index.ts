/**
 * Пайплайн изображений: sharp, размеры, форматы, pHash-дедупликация.
 *
 * Владелец пакета: агент `images-pipeline`.
 *
 * Запланировано ТЗ и CLAUDE.md, пока не реализовано:
 *   - конвертация в AVIF и WebP + резервный JPEG, оригиналы хранятся отдельно
 *     от оптимизированных копий;
 *   - генерация набора размеров под `srcset`/`sizes`;
 *   - перцептивный хеш (pHash) и поиск визуально похожих опубликованных открыток;
 *   - имя файла — короткое описательное на транслите, URL файла постоянный.
 *
 * sharp не установлен: добавляет его агент `images-pipeline` вместе с первой
 * реализацией, а не заранее.
 */

/** Форматы вывода в порядке предпочтения; последний — резервный. */
export const OUTPUT_FORMATS = ['avif', 'webp', 'jpeg'] as const;

export type OutputFormat = (typeof OUTPUT_FORMATS)[number];

/** Резервный формат для браузеров без AVIF/WebP. */
export const FALLBACK_FORMAT: OutputFormat = 'jpeg';
