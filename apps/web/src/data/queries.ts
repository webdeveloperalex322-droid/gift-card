/**
 * Запросы публичного рендера — ЧИСТЫЕ функции (задача Э3-02).
 *
 * Разделение «сборка запроса / его выполнение» сделано ради проверяемости:
 * ответы на вопросы «выключен ли access control», «не дублируется ли фильтр по
 * статусу», «устойчив ли порядок при пагинации» — это свойства ЗНАЧЕНИЙ, и
 * проверять их поднятой базой значило бы проверять редко. Выполнение живёт в
 * `./content.ts` и состоит из вызова Payload и приведения результата.
 *
 * ## Чего в этих запросах нет намеренно
 *
 *   - **фильтра по статусу.** Его ставит access control CMS
 *     (`contentReadAccess` возвращает `Where` для анонима), потому что
 *     `overrideAccess` здесь false. Написать `status: { equals: 'published' }`
 *     ещё и тут — значит завести второй источник защитного правила: пока они
 *     совпадают, лишняя строка безобидна, а когда разойдутся, разойдутся молча.
 *     Постусловие проверяется отказом (`assertPublicallyReadable`), а не
 *     фильтром;
 *   - **пересчёта путей.** Путь подборки считает и хранит CMS (поле `path` с
 *     уникальным индексом БД), путь карточки собирается из slug единственной
 *     функцией `buildCardPath` (`@otkritka/cms/seo/paths`). Здесь пути только
 *     сравниваются со СОХРАНЁННЫМ значением;
 *   - **условий индексации.** `robots`, `canonical` и право страницы быть в
 *     индексе — дело шаблона и sitemap (этап 4), а не выборки. Запрос отдаёт всё
 *     опубликованное: страница с `noindex` существует и отвечает 200, просто не
 *     идёт в индекс.
 *
 * ## Про сортировку
 *
 * У всех списков сортировка ПОЛНАЯ — последним ключом идёт `id`. Неоднозначный
 * порядок при постраничном обходе означает, что одна запись попадает на две
 * страницы, а другая не попадает ни на одну: для пагинации с постоянными URL
 * (`/page/N`) это потерянная из индекса карточка.
 */

import type { Where } from 'payload';

import { canonicalizePath } from '@otkritka/shared';
import { COLLECTION_PATH_PREFIX } from '@otkritka/cms/seo/paths';

import { assertPageNumber, DEFAULT_CARDS_PER_PAGE, PUBLIC_READ_SCOPE } from './read-scope.js';

/** Коллекции, которые читает публичный рендер. */
export type PublicCollectionSlug = 'cards' | 'collections';

/** Идентификатор записи в том виде, в каком его отдаёт Payload. */
export type RecordId = number | string;

/**
 * Запрос к коллекции в области чтения публичного рендера.
 *
 * Форма совпадает с параметрами `payload.find`, поэтому объект передаётся туда
 * как есть: промежуточного «перевода» нет, и параметр не может потеряться по
 * пути.
 */
export interface PublicFindQuery<TSlug extends PublicCollectionSlug> {
  collection: TSlug;
  depth: 0;
  limit?: number;
  overrideAccess: false;
  page?: number;
  pagination?: boolean;
  sort?: string[];
  where?: Where;
}

/**
 * Запрос ЧИСЛА записей в той же области чтения (`payload.count`).
 *
 * Отдельный тип, а не `PublicFindQuery` с `limit: 0`: у `count` нет ни `depth`,
 * ни `sort`, ни `pagination`, и передавать ему лишние поля значило бы делать вид,
 * что они на что-то влияют. `overrideAccess: false` здесь так же обязателен, как
 * в чтении: без него счётчик считал бы и черновики (`PUBLIC_READ_SCOPE`,
 * `./read-scope.ts`).
 */
export interface PublicCountQuery<TSlug extends PublicCollectionSlug> {
  collection: TSlug;
  overrideAccess: false;
  where: Where;
}

/**
 * Есть ли у узла хоть одна ОПУБЛИКОВАННАЯ открытка — половина предиката
 * «непуст».
 *
 * Считается число, а не читаются документы: наружу нужен только ответ «да или
 * нет», а выборка строк ради этого читала бы поля, которые никуда не пойдут.
 */
export function collectionCardsCountQuery(collectionId: RecordId): PublicCountQuery<'cards'> {
  return {
    collection: 'cards',
    overrideAccess: false,
    where: { collections: { in: [collectionId] } },
  };
}

/**
 * Порядок карточек в любом списке: сначала свежие по дате ПЕРВОЙ публикации,
 * затем по идентификатору.
 *
 * Почему `publishedAt`, а не `updatedAt`: правка опечатки не должна
 * перетасовывать выдачу и тем самым менять состав страниц пагинации. Почему
 * `id` вторым ключом — см. шапку модуля.
 */
const CARD_ORDER: readonly string[] = ['-publishedAt', '-id'];

/**
 * Тот же порядок карточек, прочитанный С ДРУГОГО КОНЦА.
 *
 * Нужен ровно одной выборке — половине «новее» у окна «Похожие открытки»
 * ({@link similarCardsQueries}): там предел обязан отрезать ДАЛЬНИХ соседей, а не
 * ближних, поэтому ближайший обязан прийти первой строкой. Списками и пагинацией
 * этот порядок не пользуется: там порядок показа один и он убывающий.
 *
 * Значение выведено из {@link CARD_ORDER} переворотом знака, а не написано
 * второй строкой: два независимых списка ключей однажды разошлись бы, и тогда
 * окно перестало бы быть отрезком того же порядка, в котором открытки идут в
 * списках.
 */
const CARD_ORDER_ASCENDING: readonly string[] = CARD_ORDER.map((key) =>
  key.startsWith('-') ? key.slice(1) : `-${key}`,
);

/**
 * Потолок числа строк у списков БЕЗ пагинации (дети узла, смежные, сезонные).
 *
 * Это не пагинация, а предел: `limit: 0` в Payload означает «без предела», и
 * страница со SSR однажды прочитала бы весь каталог одним запросом. Значение —
 * выбор агента (кандидат в реестр решений): столько узлов в одном блоке
 * перелинковки на странице всё равно не выводится, а превышение означает, что
 * блоку нужна своя страница со списком, а не больший предел.
 */
export const MAX_LIST_ROWS = 200;

/** Порядок узлов подборок: по заголовку, затем по идентификатору. */
const COLLECTION_ORDER: readonly string[] = ['title', 'id'];

/**
 * Сколько открыток выводит блок «Похожие открытки» (ТЗ §5.4: 6–12 шт.).
 *
 * Верхняя граница — предел ЗАПРОСА, поэтому она здесь: лишние строки, которые
 * шаблон всё равно не покажет, не читаются вовсе.
 */
export const SIMILAR_CARDS_MAX = 12;

/**
 * Нижняя граница того же диапазона — ОРИЕНТИР, а не условие запроса.
 *
 * Добить блок до шести открытками из других тем нельзя: заголовок «Похожие
 * открытки» стал бы неправдой, а разметка и видимое содержимое обязаны
 * соответствовать друг другу. Поэтому нижняя граница обеспечивается ДАННЫМИ: у
 * темы минимум 20 опубликованных открыток (решение Ч-06), значит у любой
 * карточки внутри такой темы похожих не меньше 19. Меньше шести в блоке —
 * сигнал о неполноте темы, а не о дефекте шаблона; страницу это не ломает.
 */
export const SIMILAR_CARDS_TARGET_MIN = 6;

/**
 * Сколько смежных подборок выводит блок перелинковки (ТЗ §5.3: 3–6 вбок).
 *
 * Ограничение стоит на запросе, а не на шаблоне: связь `related` редактор может
 * заполнить любым числом узлов, и без предела страница выводила бы их все.
 * Порядок при этом задаёт запрос (по заголовку), а не редактор, — блок
 * перелинковки не имеет главного элемента, в отличие от связи `collections` у
 * карточки, где первая подборка основная.
 */
export const RELATED_COLLECTIONS_MAX = 6;

/** Карточка по slug. Slug уникален в коллекции, поэтому документ ровно один. */
export function cardBySlugQuery(slug: string): PublicFindQuery<'cards'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'cards',
    limit: 1,
    pagination: false,
    where: { slug: { equals: slug } },
  };
}

/**
 * Подборка по ИТОГОВОМУ пути.
 *
 * Путь приводится к канонической форме (решение Ч-21 — без завершающего слеша)
 * единственным хелпером `canonicalizePath`, а не сравнивается «как пришло»:
 * иначе `/podborki/8-marta/` не нашёл бы запись, у которой в поле `path` лежит
 * `/podborki/8-marta`.
 *
 * @throws Error если путь лежит вне пространства подборок. Пространства имён
 *   разведены решением человека от 2026-08-22, и путь карточки здесь — ошибка
 *   вызывающего, а не пустой результат: пустой результат выглядел бы как «такой
 *   подборки нет» и прятал бы перепутанный маршрут.
 */
export function collectionByPathQuery(path: string): PublicFindQuery<'collections'> {
  const canonical = canonicalizePath(path);
  if (canonical !== COLLECTION_PATH_PREFIX && !canonical.startsWith(`${COLLECTION_PATH_PREFIX}/`)) {
    throw new Error(
      `«${canonical}» не является путём подборки: узлы таксономии живут под ` +
        `${COLLECTION_PATH_PREFIX}, карточки — под /otkrytki (решение человека от ` +
        '2026-08-22). Запрос не отправлен: коллизия пространств имён невозможна ' +
        'структурно, поэтому чужой путь здесь означает ошибку маршрута.',
    );
  }
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'collections',
    limit: 1,
    pagination: false,
    where: { path: { equals: canonical } },
  };
}

export interface CollectionCardsQueryInput {
  readonly collectionId: RecordId;
  /** Номер страницы, начиная с 1. `/page/1` не существует (решение Ч-05). */
  readonly page: number;
  readonly perPage?: number;
}

/**
 * Карточки подборки, страница за страницей.
 *
 * Связь m:n живёт в карточке (`cards.collections`), поэтому фильтр идёт по ней —
 * копий карточки внутри подборок не существует, и канонический адрес карточки
 * остаётся один (`/otkrytki/<slug>`).
 */
export function collectionCardsQuery(input: CollectionCardsQueryInput): PublicFindQuery<'cards'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'cards',
    limit: input.perPage ?? DEFAULT_CARDS_PER_PAGE,
    page: assertPageNumber(input.page),
    sort: [...CARD_ORDER],
    where: { collections: { in: [input.collectionId] } },
  };
}

export interface CatalogCardsQueryInput {
  /** Номер страницы, начиная с 1. `/page/1` не существует (решение Ч-05). */
  readonly page: number;
  readonly perPage?: number;
}

/**
 * Карточки КАТАЛОГА `/otkrytki`, страница за страницей (задача Э3-08).
 *
 * Отличается от {@link collectionCardsQuery} ровно отсутствием фильтра по
 * подборке: каталог перечисляет всё опубликованное. Порядок тот же и это
 * обязательно — иначе одна и та же карточка попадала бы на две страницы каталога.
 *
 * Почему это не {@link recentCardsQuery} с параметром: у того пагинации нет
 * вовсе (`pagination: false`, предел строк), он отвечает на другой вопрос —
 * «свежие для блока», где число известно заранее. Один запрос на две роли
 * означал бы, что блок главной однажды поедет по страницам, а каталог перестанет
 * знать общее число открыток.
 */
export function catalogCardsQuery(input: CatalogCardsQueryInput): PublicFindQuery<'cards'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'cards',
    limit: input.perPage ?? DEFAULT_CARDS_PER_PAGE,
    page: assertPageNumber(input.page),
    sort: [...CARD_ORDER],
  };
}

/* ------------------------------------------------------------------ */
/* Обход ВСЕГО опубликованного: карта сайта (задача Э4-04)             */
/* ------------------------------------------------------------------ */

/**
 * Размер пачки при обходе коллекции целиком.
 *
 * Это не пагинация и не предел выдачи: карта сайта обязана перечислить ВСЁ, и
 * единственный вопрос — сколько строк читать за один запрос. Значение — выбор
 * агента (кандидат в реестр решений): пачка меньше даёт лишние обращения к базе,
 * пачка больше — заметный пик памяти на одном ответе. Верхняя граница числа
 * адресов в одном ФАЙЛЕ карты сайта к этому значению отношения не имеет: она
 * равна 50 000 и живёт в `../seo/sitemap.ts`.
 */
export const SITEMAP_SCAN_BATCH = 500;

/**
 * Страница обхода карточек для карты сайта.
 *
 * Отдельная функция, а не {@link catalogCardsQuery} с большим пределом, и
 * причина смысловая: у каталога `perPage` — это ВИДИМАЯ страница списка, её
 * значение выбирает человек и от него зависит состав `/otkrytki/page/N`.
 * Связать с ним размер пачки обхода значило бы менять число запросов к базе при
 * правке вёрстки — и наоборот, менять состав видимых страниц, «оптимизируя»
 * обход.
 *
 * Порядок тот же, что у всех списков, и он обязателен: при постраничном обходе
 * неоднозначный порядок означает карточку, попавшую в карту дважды, и другую, не
 * попавшую вовсе.
 */
export function sitemapCardsQuery(page: number): PublicFindQuery<'cards'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'cards',
    limit: SITEMAP_SCAN_BATCH,
    page: assertPageNumber(page),
    sort: [...CARD_ORDER],
  };
}

/** Страница обхода узлов таксономии для карты сайта. Порядок — общий. */
export function sitemapCollectionsQuery(page: number): PublicFindQuery<'collections'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'collections',
    limit: SITEMAP_SCAN_BATCH,
    page: assertPageNumber(page),
    sort: [...COLLECTION_ORDER],
  };
}

/**
 * Узлы верхнего уровня таксономии — содержание каталога `/podborki` (Э3-08).
 *
 * «Верхний уровень» — это отсутствие родителя, а не вид узла: группирующие узлы
 * (`prazdniki`, `adresaty`) и адресаты без праздника лежат на первом уровне
 * одинаково, а завязка на `nodeKind` означала бы, что каталог теряет раздел при
 * первом же новом виде узла.
 *
 * Предел — {@link MAX_LIST_ROWS}: это не пагинация, а граница SSR-запроса.
 * Каталог `/podborki` пагинации не имеет намеренно — узлов верхнего уровня
 * единицы, и страница со списком разделов постранично не разбивается.
 */
export function rootCollectionsQuery(): PublicFindQuery<'collections'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'collections',
    limit: MAX_LIST_ROWS,
    pagination: false,
    sort: [...COLLECTION_ORDER],
    where: { parent: { exists: false } },
  };
}

/**
 * Дочерние узлы подборки.
 *
 * `null` вместо запроса, если родителя нет: `{ in: [] }` часть адаптеров
 * трактует как отсутствие условия, то есть отдала бы ВСЕ подборки. Пустой набор
 * обязан быть пустым ответом, а не выборкой каталога.
 */
export function childCollectionsQuery(
  parentId: RecordId | null,
): PublicFindQuery<'collections'> | null {
  if (parentId === null) {
    return null;
  }
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'collections',
    limit: MAX_LIST_ROWS,
    pagination: false,
    sort: [...COLLECTION_ORDER],
    where: { parent: { in: [parentId] } },
  };
}

/**
 * Подборка по идентификатору.
 *
 * Запрос списком, а не `findByID`, ровно по одной причине: неопубликованная
 * запись обязана прийти ПУСТЫМ результатом, а не исключением о запрете доступа.
 * Различать «нет записи» и «нельзя читать» публичному рендеру нечем и незачем:
 * ссылки на неё всё равно не будет.
 *
 * Потребителей два, и оба — крошки (Э3-03): родитель узла при обходе цепочки и
 * ОСНОВНАЯ подборка карточки (первая в связи `cards.collections`, ТЗ §5.4).
 * Поэтому запрос назван по тому, что он делает, а не по одному из двух случаев:
 * имя «родительский узел» заставило бы второго потребителя либо звать функцию
 * не по смыслу, либо завести рядом её копию.
 */
export function collectionByIdQuery(
  id: RecordId | null,
): PublicFindQuery<'collections'> | null {
  if (id === null) {
    return null;
  }
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'collections',
    limit: 1,
    pagination: false,
    where: { id: { in: [id] } },
  };
}

/**
 * Подборки по набору идентификаторов.
 *
 * Потребителей два, и предел у них разный, поэтому он аргумент, а не константа
 * внутри: блок смежных подборок выводит не больше {@link RELATED_COLLECTIONS_MAX}
 * узлов (ТЗ §5.3), а связь `collections` карточки читается ЦЕЛИКОМ — из неё
 * получаются видимые атрибуты-ссылки (повод, адресат), и обрезать их числом
 * значило бы спрятать часть привязок открытки.
 *
 * Порядок ответа — по заголовку. Там, где значим порядок, заданный редактором
 * (связь `collections` карточки: первая подборка — основная), его восстанавливает
 * `orderByIds` из `./relations.ts`: сортировать по списку идентификаторов на
 * стороне БД нечем.
 */
export function collectionsByIdsQuery(
  ids: readonly RecordId[],
  limit?: number,
): PublicFindQuery<'collections'> | null {
  if (ids.length === 0) {
    return null;
  }
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'collections',
    limit: limit === undefined ? ids.length : Math.min(ids.length, limit),
    pagination: false,
    sort: [...COLLECTION_ORDER],
    where: { id: { in: [...ids] } },
  };
}

/** Смежные подборки для блока перелинковки: не больше шести (ТЗ §5.3). */
export function relatedCollectionsQuery(
  ids: readonly RecordId[],
): PublicFindQuery<'collections'> | null {
  return collectionsByIdsQuery(ids, RELATED_COLLECTIONS_MAX);
}

export interface SimilarCardsQueryInput {
  /** Подборки, по которым ищется похожее: связь `collections` самой карточки. */
  readonly collectionIds: readonly RecordId[];
  /** Идентификатор самой карточки: на себя блок «Похожие» не ссылается. */
  readonly excludeCardId: RecordId;
  /**
   * `publishedAt` САМОЙ карточки — центр окна соседей.
   *
   * Обязательный параметр без значения по умолчанию: именно он делает выборку
   * зависящей от карточки, и забывчивость обязана быть ошибкой типов, а не
   * молчаливым возвратом к общему для всей темы списку. У опубликованной записи
   * поле заполнено всегда — его ставит хук первой публикации
   * (`apps/cms/src/collections/content-hooks.ts`), а публичный рендер читает
   * только опубликованное. `null` здесь означает повреждённую запись, и
   * поведение для него описано в {@link similarCardsQueries}.
   */
  readonly publishedAt: string | null;
  readonly limit?: number;
}

/**
 * Пара запросов окна «Похожие открытки»: соседи новее и соседи старше.
 *
 * Оба — от БЛИЖАЙШЕГО соседа к более далёкому, поэтому у них разный порядок
 * сортировки. Склеивает их {@link similarCardsWindow}.
 */
export interface SimilarCardsQueries {
  /** Соседи НОВЕЕ карточки, от ближайшего. `null` — спрашивать нечего. */
  readonly newer: PublicFindQuery<'cards'> | null;
  /** Соседи СТАРШЕ карточки, от ближайшего. `null` — спрашивать нечего. */
  readonly older: PublicFindQuery<'cards'> | null;
}

/**
 * Общая часть обеих половин окна: та же тема, но не сама карточка.
 *
 * Исключение самой карточки делает ЗАПРОС, а не шаблон: карточка,
 * отфильтрованная после выборки, уменьшала бы блок на одну позицию
 * непредсказуемо.
 */
function sameThemeExceptSelf(input: SimilarCardsQueryInput): Where {
  return {
    and: [
      { collections: { in: [...input.collectionIds] } },
      { id: { not_equals: input.excludeCardId } },
    ],
  };
}

/**
 * Строгое сравнение с карточкой по ключу порядка `(publishedAt, id)`.
 *
 * Сравнение СОСТАВНОЕ, и это не педантизм: у пачки открыток, опубликованных
 * одной операцией (пакетная публикация разрешена решением Ч-07), `publishedAt`
 * совпадает с точностью до миллисекунды. Сравнение по одной дате разрезало бы
 * такую пачку не пополам, а целиком в одну сторону — и половина окна оказалась
 * бы пустой у всех карточек пачки сразу. Второй ключ — `id`, тот же, которым
 * доводится до полного порядок списков (см. шапку модуля).
 */
function beyondCard(
  input: SimilarCardsQueryInput & { readonly publishedAt: string },
  direction: 'newer' | 'older',
): Where {
  const byDate =
    direction === 'newer'
      ? { publishedAt: { greater_than: input.publishedAt } }
      : { publishedAt: { less_than: input.publishedAt } };
  const byId =
    direction === 'newer'
      ? { id: { greater_than: input.excludeCardId } }
      : { id: { less_than: input.excludeCardId } };
  return {
    or: [byDate, { and: [{ publishedAt: { equals: input.publishedAt } }, byId] }],
  };
}

/**
 * Похожие открытки: ОКНО СОСЕДЕЙ карточки из тех же подборок (ТЗ §5.4).
 *
 * «По общим подборкам и атрибутам» из ТЗ здесь означает ровно общие подборки:
 * отдельных полей стиля и настроения у карточки в схеме нет — по решению Ч-04-3
 * это фильтр без собственных URL, а привязка к поводу и адресату выражена именно
 * связью `collections`.
 *
 * ## Почему окно, а не «двенадцать свежих темы»
 *
 * Прежняя версия отдавала один и тот же запрос ВСЕМ карточкам темы: свежие
 * двенадцать минус сама карточка. Тогда карточка с тринадцатой по счёту и дальше
 * не входила НИ В ОДИН блок «Похожие» — а именно на этот блок решение Ч-04-8
 * возлагает достижимость карточек за первой страницей списка (находка ревизии
 * Э3-05/Э3-06, MAJOR). Окно соседей это исправляет: блок карточки — непрерывный
 * отрезок общего порядка вокруг неё, поэтому «быть соседом» — отношение
 * взаимное, и каждая карточка попадает в блок своего соседа.
 *
 * ## ГРАНИЦА ЭТОЙ ГАРАНТИИ (сужена по вердикту `reviewer`, MINOR 6)
 *
 * Взаимность держится ВНУТРИ ОДНОГО ПУЛА соседей, а пул — это объединение
 * подборок карточки (`collections: { in: [...] }`). У двух карточек с РАЗНЫМИ
 * наборами подборок пулы разные, и отношение перестаёт быть взаимным: карточка
 * малой подборки, чей единственный сосед входит ещё и в большую, не попадает в
 * его двенадцать позиций, хотя он попадает в её. Тогда входящей ссылки «Похожие»
 * у неё нет ни от кого. Случай воспроизведён тестом
 * (`./data-access.test.ts`, «при РАЗНЫХ наборах подборок „быть соседом“
 * перестаёт быть взаимным») — он фиксирует границу, а не проходит мимо неё.
 *
 * Поэтому формулировка гарантии такая: блок ВСЕГДА даёт исходящие ссылки на
 * непосредственных соседей карточки в её собственном пуле, а утверждение «у
 * каждой карточки есть входящая ссылка» доказано для карточек с ОДИНАКОВЫМ
 * набором подборок (тест на наборе из 37 карточек, заметно большем предела
 * блока). Полное покрытие при разнородных наборах требует считать окно по
 * КАЖДОЙ подборке карточки отдельно — тогда взаимность восстанавливается внутри
 * каждой подборки, а цена растёт с числом подборок у карточки (два запроса на
 * подборку вместо двух на карточку). Это изменение запроса, а не формулировки, и
 * оно вынесено в отчёт задачи, а не сделано молча.
 *
 * До него достижимость таких карточек держится списком подборки и его
 * пагинацией: страницы 2+ отдают `noindex,follow`, то есть ссылки на них
 * обходятся.
 *
 * Порядок один и тот же во всём проекте — `(publishedAt, id)` по убыванию (см.
 * шапку модуля), поэтому «сосед» здесь означает соседа в том же порядке, в
 * котором открытки идут в списках и в пагинации.
 *
 * ## Что происходит при пустых значениях
 *
 *   - пустой набор подборок — оба запроса `null`: `{ in: [] }` часть адаптеров
 *     трактует как отсутствие условия, то есть в блок «похожих» попал бы весь
 *     каталог;
 *   - `publishedAt` карточки пуст (повреждённая запись — у опубликованной поле
 *     ставит хук) — окна нет, и половина «старше» вырождается в прежнюю выборку
 *     по всей теме. Это осознанная деградация до предыдущего поведения: страница
 *     остаётся рабочей, а достижимость держится пагинацией, которая у списка
 *     теперь есть. Отказ здесь был бы хуже — 500 на странице, которую человек
 *     опубликовал.
 */
export function similarCardsQueries(input: SimilarCardsQueryInput): SimilarCardsQueries {
  if (input.collectionIds.length === 0) {
    return { newer: null, older: null };
  }

  const limit = input.limit ?? SIMILAR_CARDS_MAX;
  const theme = sameThemeExceptSelf(input);
  // Предел ставится на КАЖДУЮ половину: соседей с одной стороны может не быть
  // вовсе (самая свежая открытка темы), и тогда блок добирается с другой.
  // Лишние строки при этом не читаются — половина ограничена размером блока.
  const half = (order: readonly string[], where: Where): PublicFindQuery<'cards'> => ({
    ...PUBLIC_READ_SCOPE,
    collection: 'cards',
    limit,
    pagination: false,
    sort: [...order],
    where,
  });

  if (input.publishedAt === null) {
    return { newer: null, older: half(CARD_ORDER, theme) };
  }

  const centred = { ...input, publishedAt: input.publishedAt };
  return {
    // Половина «новее» читается ВОЗРАСТАЮЩИМ порядком: ближайший сосед обязан
    // прийти первым, иначе предел отрезал бы как раз соседей, а оставил самые
    // свежие открытки темы — то есть вернул бы прежний дефект.
    newer: half(CARD_ORDER_ASCENDING, { and: [theme, beyondCard(centred, 'newer')] }),
    older: half(CARD_ORDER, { and: [theme, beyondCard(centred, 'older')] }),
  };
}

/**
 * Склейка двух половин окна в один блок «Похожие открытки».
 *
 * Порядок результата — тот же убывающий порядок сайта: сначала соседи новее (от
 * дальнего к ближайшему), затем соседи старше от ближайшего. Получается
 * непрерывный отрезок общего порядка с вырезанной серединой — самой карточкой.
 *
 * Распределение предела между половинами: пополам, а недобранное одной стороной
 * добирает другая. Из этого правила следует, что при пределе не меньше двух
 * каждая непустая половина получает хотя бы одну позицию, поэтому
 * непосредственный сосед карточки в её пуле попадает в её блок ВСЕГДА. Обратное
 * («а значит и она — в его») верно, только если пул у соседа тот же, то есть при
 * совпадающих наборах подборок; граница разобрана в шапке
 * {@link similarCardsQueries}.
 *
 * Функция обобщённая, потому что о карточках ей знать нечего: она распределяет
 * позиции между двумя упорядоченными списками. Так её проверяет тест без записей
 * CMS и без базы.
 *
 * @throws Error если предел меньше двух. Предел 1 отдал бы всю позицию одной
 *   стороне, и взаимность «быть соседом» — а вместе с ней достижимость —
 *   перестала бы держаться по построению. Молча выдать одну ссылку вместо
 *   гарантии нельзя: пропажу обнаружил бы не тест, а отсутствие страниц в
 *   индексе.
 */
export function similarCardsWindow<TItem>(input: {
  /** Соседи новее, от ближайшего к дальнему. */
  readonly newer: readonly TItem[];
  /** Соседи старше, от ближайшего к дальнему. */
  readonly older: readonly TItem[];
  readonly limit?: number;
}): readonly TItem[] {
  const limit = input.limit ?? SIMILAR_CARDS_MAX;
  if (!Number.isInteger(limit) || limit < 2) {
    throw new Error(
      `Предел блока «Похожие открытки» равен ${String(limit)}, а окно соседей требует минимум ` +
        'двух позиций: при одной вся позиция уходит одной стороне, и карточка перестаёт ' +
        'попадать в блок своего соседа. На этом отношении держится достижимость карточек за ' +
        'первой страницей списка (решение Ч-04-8).',
    );
  }

  const half = Math.floor(limit / 2);
  const fromNewer = Math.min(input.newer.length, half);
  const fromOlder = Math.min(input.older.length, limit - fromNewer);
  // Второй проход по половине «новее»: если соседей старше меньше, чем им
  // отвели, освободившиеся позиции достаются ей. Без этого блок у самой старой
  // открытки темы состоял бы из шести ссылок вместо двенадцати.
  const newerTaken = input.newer.slice(0, Math.min(input.newer.length, limit - fromOlder));

  return [...newerTaken].reverse().concat(input.older.slice(0, fromOlder));
}

/**
 * Внутренний поиск: открытки по названию, подписи и описанию (ТЗ §5.5).
 *
 * Оператор `like` — это подстрока без учёта регистра. Морфологии у него нет, и
 * подменять её здесь нечем: полнотекстовый индекс со словарём — отдельная работа
 * с миграцией схемы (этап 7), а самодельная «нормализация окончаний» ошибалась бы
 * молча. Поэтому поиск честно ищет вхождение строки, а страница результатов
 * прямо предлагает уточнить запрос.
 *
 * Значение уходит ПАРАМЕТРОМ запроса — подстановки строк в SQL здесь нет.
 * Подстановочные символы `%` и `_` внутри запроса не вычищаются: они расширяют
 * поиск, а не выходят за его пределы.
 *
 * Предел стоит на запросе: страницы результатов у поиска нет намеренно
 * (`../seo/search-page.ts`, `SEARCH_RESULTS_LIMIT`), поэтому лишние строки не
 * читаются вовсе. Порядок — общий порядок списков: свежие первыми.
 */
export function searchCardsQuery(input: {
  readonly query: string;
  readonly limit: number;
}): PublicFindQuery<'cards'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'cards',
    limit: input.limit,
    pagination: false,
    sort: [...CARD_ORDER],
    where: {
      or: [
        { title: { like: input.query } },
        { h1: { like: input.query } },
        { caption: { like: input.query } },
        { description: { like: input.query } },
      ],
    },
  };
}

/**
 * Внутренний поиск: подборки по заголовку (ТЗ §5.5 — «поиск по … атрибутам»).
 *
 * Атрибут открытки в этой модели данных — это её узел таксономии (повод,
 * адресат): отдельных полей `occasion`/`recipient` у карточки нет намеренно
 * (см. шапку `apps/cms/src/collections/cards.ts`). Поэтому поиск по атрибуту —
 * это поиск по подборкам, и найденная подборка ведёт на свою обычную страницу.
 */
export function searchCollectionsQuery(input: {
  readonly query: string;
  readonly limit: number;
}): PublicFindQuery<'collections'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'collections',
    limit: input.limit,
    pagination: false,
    sort: [...COLLECTION_ORDER],
    where: {
      or: [
        { title: { like: input.query } },
        { h1: { like: input.query } },
        { metaDescription: { like: input.query } },
      ],
    },
  };
}

/** Свежие карточки: блок главной и перелинковка. Порядок тот же, что в списках. */
export function recentCardsQuery(limit: number): PublicFindQuery<'cards'> {
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'cards',
    limit,
    pagination: false,
    sort: [...CARD_ORDER],
  };
}

/**
 * Сезонные подборки: те, чьё окно показа накрывает указанный день.
 *
 * Окно переключает БЛОК на главной (ТЗ §8.1) и не создаёт отдельных URL — ни
 * `/2026/`, ни года в адресе (решение Ч-04-4). Узел без обеих дат в блок не
 * попадает: пустое поле здесь означает «показывать не по календарю», а
 * догадываться за редактора нельзя.
 */
export function seasonalCollectionsQuery(today: Date): PublicFindQuery<'collections'> {
  const day = today.toISOString();
  return {
    ...PUBLIC_READ_SCOPE,
    collection: 'collections',
    limit: MAX_LIST_ROWS,
    pagination: false,
    sort: ['seasonal.holidayDate', 'id'],
    where: {
      and: [
        { 'seasonal.showFrom': { less_than_equal: day } },
        { 'seasonal.showUntil': { greater_than_equal: day } },
      ],
    },
  };
}
